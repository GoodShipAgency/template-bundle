{% extends "@MashboTemplate/components/alerts/notice.html.twig" %}

{% block main %}
    {% if (message|default('')|trim is not empty) or (messages|default([])|length > 0) %}
        {{ parent() }}
    {% endif %}
{% endblock %}